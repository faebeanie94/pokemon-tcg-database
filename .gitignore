# dependencies
/node_modules

# next.js
/.next/
/out/

# production, and the card database built into it by `python -m pokedb build`
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# sqlite side files, wherever POKEDB_DB points
*.db-wal
*.db-shm
/data/*.db

# python
.venv/
__pycache__/
*.pyc

# downloaded source data (re-fetched by `python -m pokedb fetch`)
data/raw/
# but keep the curated sports seed (no public checklist API)
!data/raw/sports/
!data/raw/sports/seed.json

# misc
.DS_Store
*.log
.env*.local
~$*.xlsx

# editor settings
.idea/
